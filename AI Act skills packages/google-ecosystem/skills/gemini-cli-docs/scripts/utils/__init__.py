"""
Compatibility wrappers for docs-management utility modules.

Tests and tools may import from `scripts.utils.<module>`; these thin
modules re-export the canonical implementations from the parent
`scripts` package so we do not duplicate logic.
"""


